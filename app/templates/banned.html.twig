{% extends 'base.html.twig' %}

{% block title %}Compte suspendu{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-center mt-5">
        <div class="col-md-8">
            <div class="card border-danger">
                <div class="card-header bg-danger text-white">
                    <h1 class="text-center mb-0">Compte suspendu</h1>
                </div>
                <div class="card-body">
                    <div class="alert alert-danger">
                        <h4 class="alert-heading">Accès restreint</h4>
                        <p>Votre compte a été suspendu par un administrateur. Vous n'avez plus accès aux fonctionnalités du site.</p>
                        <p>Si vous pensez qu'il s'agit d'une erreur, veuillez contacter l'administration.</p>
                    </div>
                    
                    <div class="text-center mt-4">
                        <a href="{{ path('app_logout') }}" class="btn btn-secondary">Se déconnecter</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}